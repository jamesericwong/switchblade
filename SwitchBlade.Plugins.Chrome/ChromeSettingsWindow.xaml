<Window
    x:Class="SwitchBlade.Plugins.Chrome.ChromeSettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="24,24,24,12" Spacing="4">
            <TextBlock Text="Chrome Tab Finder Settings" Style="{ThemeResource TitleTextBlockStyle}"/>
            <TextBlock Text="Add executable names (without .exe) to scan for tabs." Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="24,0,24,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ListBox x:Name="ProcessList" Grid.Row="0" Margin="0,0,0,12" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="4">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Margin="0,4"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Grid Grid.Row="1" ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="NewProcessTextBox" PlaceholderText="Process Name (e.g. chrome)" VerticalAlignment="Center"/>
                <Button Content="Add" Grid.Column="1" Click="AddButton_Click"/>
                <Button Content="Remove" Grid.Column="2" Click="RemoveButton_Click"/>
            </Grid>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,1,0,0" Padding="24">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                <Button Content="Close" Click="CloseButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
