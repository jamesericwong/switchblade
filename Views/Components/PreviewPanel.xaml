<UserControl x:Class="SwitchBlade.Views.Components.PreviewPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="300">
    <Border x:Name="MainBorder"
            Background="Black"
            Opacity="0.8"
            CornerRadius="8"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Visibility" Value="Collapsed"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding EnablePreviews}" Value="True">
                        <Setter Property="Visibility" Value="Visible"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- This Canvas is the target for DWM Thumbnail -->
            <Canvas x:Name="ThumbnailCanvas" Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
            
            <!-- Preview Title Bar -->
            <Border Grid.Row="1" 
                    Background="#CC1A1A1A" 
                    CornerRadius="0,0,7,7"
                    Padding="10,6">
                <TextBlock Foreground="#E0E0E0" FontSize="12" TextTrimming="CharacterEllipsis">
                    <Run Text="Previewing: " FontWeight="SemiBold" Foreground="#AAAAAA"/><Run Text="{Binding SelectedWindow.Title}"/>
                </TextBlock>
            </Border>
        </Grid>
    </Border>
</UserControl>
